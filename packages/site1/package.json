{
  "name": "@practice-gatsby-monorepo/site1",
  "version": "0.1.0",
  "dependencies": {
    "@practice-gatsby-monorepo/shared": "0.1.0"
  },
  "devDependencies": {
    "gatsby": "^2.23.12",
    "prettier": "2.0.5"
  },
  "scripts": {
    "build": "gatsby build",
    "develop": "gatsby develop",
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,json,md}\"",
    "start": "npm run develop",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "test": "echo \"Write tests! -> https://gatsby.dev/unit-testing\" && exit 1"
  }
}
